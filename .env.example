# Copy to .env and fill in production or personal development values.
# DO NOT store real secrets in version control.

# Database connection URL (Postgres):
# e.g.
# DATABASE_URL=postgresql://<user>:<password>@<host>:5432/<db_name>?sslmode=require

# Example (REPLACE with your own credentials):
# DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD@ep-.../neondb?sslmode=require&channel_binding=require

# Redis URL (if you use external Redis):
# REDIS_URL=redis://:<password>@<host>:6379/1

# Secret key for Django (use unique value in production):
# SECRET_KEY=replace-this-with-a-very-secret-value

# Debug (1 for True, 0 for False)
# DEBUG=0
